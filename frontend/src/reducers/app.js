
const initialState = {
    activePost: null,
    activeCategory: null,
  };
  
const app = ( state = initialState,  action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default app;


